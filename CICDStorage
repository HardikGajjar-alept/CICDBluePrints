{
  "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",

  "parameters": {
    
    "param_mystorageaccountname": {
      "type": "string",
      "minLength": 5
    },
    "param_environment": {
      "type": "string",
      "defaultValue": "dev",
      "allowedValues": [
        "dev",
        "test",
        "prod"
      ]
    }
  },

  "variables": {
    "mystorageaccountname": "[concat(parameters('param_mystorageaccountname'), parameters('param_environment'))]"
  },

  "resources": [
    {
      "type": "Microsoft.Storage/storageAccounts",
      "name": "CICDStorage",
      "location": "West US 2",
      "apiVersion": "2018-07-01",
      "kind": "StorageV2",
      "sku": {
        "name": "Standard_LRS"
      },
      "properties": {
        "accessTier": "Hot"
      }
    }
  ],

  "outputs": {
    "mystorageaccount": {
      "type": "object",
      "value": "[reference(variables('mystorageaccountname'))]"
    }
  }
}
